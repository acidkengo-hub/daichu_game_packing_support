# DEVLOG — DAICHU Game Packing Support

変更履歴と判断の記録。**新しい記録を上に追記していく。**

「なぜこの実装になっているのか」を半年後の自分が追えるようにするための文書。
採用しなかった案とその理由、そして**誤った判断とその訂正**も残す。

---

## 2026-08-10 ドキュメント整備

### 何を作ったか

- 現場向け PDF マニュアル（全25ページ）
- `README.md`（現在の構造のスナップショット）
- `DEVLOG.md`（本ファイル）

### PDFマニュアルの技術的な判断

**ReportLab + NotoSansJP TTF を採用。**
日本語PDFの生成には、フォントの埋め込みが必須。
NotoSansJP の Variable フォント（TTF）を Google Fonts の GitHub から取得した。

採用しなかった案:
- **CFF/OTF形式のフォント** → ReportLab の `TTFont` が読めない
- **HTML → PDF 変換（weasyprint等）** → 依存が重く、日本語の行分割制御が効きにくい

**絵文字は使わない。**
最初 📦 🎮 などの絵文字を多用したが、NotoSansJP に絵文字グリフが無く、
すべて豆腐（□）で描画された。

`fontTools` で cmap を検査して使える記号を確認した結果:

```
使える:    ⚠ ▶ ◀ ✓ ● ○ ■ □ ◆ ★ ☆ ← → ‹ › ※ ① ② ③ ④
使えない:  📦 🎮 🔍 📷 🌇 🌅 ✉ ⚙ 📇 💡 ✅ ✔ ⏸
```

以降は `■` `●` `▲` `★` などの幾何学記号で代用している。
**PDFに新しい記号を追加するときは、必ず cmap を検査すること。**

### ⚠ 誤った判断と訂正

**スクリーンショットの対応付けを間違えた。**
草稿に書かれたスクショ番号を「1つずれている」と誤って判断し、
全画像を1つ前倒しで配置してしまった。

実際には**草稿の番号が正しかった**。
画像の中身を1枚ずつ確認せず、途中まで見た印象で全体を推測したのが原因。

さらにその後、修正したつもりが「説明文と画像が別ページに分かれる」状態を作り、
見た目上まだずれているという指摘を受けた。

**訂正**: 説明文と対応画像を `KeepTogether` で必ず同じページに固定した。
セクション見出しも最初のステップと結合し、見出しだけのページが出ないようにした。

**学び**: 画像とテキストの対応は、レンダリング結果を実際に目視で確認する。
番号の一致だけで判断しない。

---

## 2026-08-09 Switch 2 対応 / DS系おまけソフト修正

### 何を変えたか

- DS初期型・DSi・DSi LL の3セットに `おまけソフト(DS)` を追加
- Switch 2 のセット定義を追加（商品コード `2025122601`）
- `PLATFORMS` に `Switch2` を追加

### なぜ変えたか

現場から「DSiLLのすぐ遊べるセットにおまけソフトの表示が抜けている」と報告があった。
確認すると DS Lite のみ定義されており、他3セットで漏れていた。

### 判断の根拠

**Switch2 の判定は Switch より先に評価する。**
`snL.includes("switch")` が先にあると「Switch2」も `Switch` にマッチしてしまう。

検証コードを書いて6ケースすべて通ることを確認した:

```
"スイッチ２ すぐ遊べる" / "2025122601" → Switch2 ✓
"スイッチすぐ遊べる"   / "switch-sugu-set" → Switch ✓
"スイッチアダプター"   / "switchacadapter" → Switch ✓
```

**Switch 2 の同梱物は属性1で3通りに分岐する。**
「外箱付き」「本体のみ」「外箱なし」がカラー選択と同じ欄で選ばれるため、
セット定義を3つ作るのではなく `parsers.ts` で動的に切り替える方式にした。

採用しなかった案:
- **セット定義を3件に分割** → 商品コードが同一なので `findSetDefinition` で区別できない
- **属性ごとに `codes` を分ける** → CSVには属性が別列で来るため、コードだけでは判別不能

Wii の電池セット（属性1の「4本」で数量を変える）と同じパターンで統一した。

---

## 2026-08-09 UI/UX リデザイン

### 何を変えたか

- 梱包画面のナビゲーションを再配置（移動＝上部の矢印、完了＝下部のボタン）
- `ShipmentSession` に `packingDone`（完了した管理番号の配列）を追加
- 便カードに進捗バーと「中断した場所から再開」ボタンを追加
- 初回のみ表示される説明バナーを追加

### なぜ変えたか

現場テストで以下の指摘を受けた。

1. 「梱包完了」と「次の注文」の区別がつかず、完了せずに進んでしまいそう
2. どこまで梱包が進んだか、どこで中断したかが分からない

### 判断の根拠 ★重要

**進捗管理の設計が根本的に間違っていた。**

変更前は `packingIdx`（現在何番目を見ているか）だけで進捗を管理していた。
これでは以下が表現できない:

- 5番目まで完了したのか、5番目を見ているだけなのか
- 前の注文に戻ったとき、完了情報が失われる
- 中断位置（次にやるべき注文）が特定できない

**「現在位置」ではなく「完了記録」を持つべきだった。**
`packingDone: Record<string, string[]>` でキャリアごとに完了した管理番号を配列で保持する方式に変更。
これにより前後移動しても完了件数が変わらず、`resumeAt`（最初の未完了注文）も算出できる。

**移動と完了を物理的に分離した。**
「次の注文」ボタンが下部に並んでいると、完了ボタンと見間違えて素通りできてしまう。
移動を上部の `‹` `›` のみにすることで、**構造的に**その事故が起きないようにした。
ボタンの色や文言を変えるだけでは不十分と判断した。

**完了済み注文は再編集可能にした。**
読み取り専用にする案もあったが、間違えて完了にしたときに修正できないと詰む。
完了済みは緑枠＋バッジで明示し、タップで取り消せるようにした。

---

## 2026-08-08 午後便対応

### 何を変えたか

- `shipmentStore.ts` を新規作成
- 午前便・午後便を `WorkDay` という単位で localStorage に永続化
- ホーム画面を便選択カード方式に再構成

### なぜ変えたか

DAICHU には13時ごろの「午後便」がある。
しかし CROSS MALL は午後便だけのCSVを出力できず、13時に出力すると午前便を含む全件が出る。
従来はアップロードのたびに全上書きしていたため、午前便の進捗が消えてしまう。

### 判断の根拠

**差分検出の基準は管理番号の差集合。**

```
新CSVの管理番号 − 既存の管理番号 = 午後便
```

管理番号は連番で発行されるため誤検出がない。

採用しなかった案:
- **ショップ名でフィルタ（Amazon/楽天のみ）** → 他モールの追加注文を取りこぼす。
  結果的にAmazon・楽天が主になるが、それは結果であって条件にすべきではない
- **注文日時でフィルタ** → 決済タイミングのずれで漏れる。実際に「1週間前の注文が昨日決済された」ケースがある

**日付が変わったら自動リセット。**
`WorkDay.date` に日付を持たせ、`loadWorkDay()` で今日と照合する。
前日データが残り続けて誤操作につながるのを防ぐ。

**既存stateを活かす方式を選んだ。**
`pickingChecked` などを全て `workDay[activeSlot].pickingChecked` に置き換える案もあったが、
App.tsx 全体の参照箇所を書き換える必要があり、リスクが高い。

ローカルstateはそのまま残し、便・キャリア選択時にセッションから復元、
変更時に `useEffect` でセッションへ書き戻す方式にした。変更範囲を最小化できた。

---

## 2026-08-08 モール色分け / DS系カラーランダム対応

### 何を変えたか

- `shopColors.ts` を新規作成。モール別の枠色・バッジ色・チラシ要否を定義
- 梱包画面に画面全体を囲む太枠を追加
- 楽天・Yahoo の注文にチラシ確認アラートを追加（設定でON/OFF可）
- 属性1に「ランダム」を含む DS/3DS系セットは、おまけソフトを2本に

### なぜ変えたか

現場（樋渡さん）から以下の意見が出た。

- DS系のカラーランダムはおまけソフトが1本ではなく2本
- 楽天とヤフショの注文だけ分かるようにしてほしい（チラシを入れるため）

### 判断の根拠

**Tailwind のクラス名は完全な文字列で書く。**
`border-${color}-500` のような動的生成はビルド時の静的解析で拾えないため、
`SHOP_STYLES` には完成形のクラス名を直接書いている。

**色分けだけでなくチェック付きアラートも追加した。**
色分けだけだと「気づいたが入れ忘れた」が起きうる。
タッチペン確認と同じくチェック必須にすることで、確実に確認させる。

キャンペーンは時期で変わるため、設定画面でON/OFFできるようにした。

**おまけソフトの数量は動的計算にした。**
セット定義を「ランダム用」「指定色用」に分ける案もあったが、
商品コードが同一なので区別できない。
`parsers.ts` で属性1に「ランダム」が含まれるかを見て `qty: 2` に上書きする方式にした。

---

## 2026-08-08 商品画像ビューア（F2）

### 何を変えたか

- `imageMapping.ts` を新規作成。139セット中136件に画像を紐付け
- 梱包画面の商品ヘッダに📷ボタンを追加
- ピッキングのカードモードにも画像を出したが、**後に削除**

### 判断の根拠

**画像はリポジトリに入れず、Google Drive を参照する。**
141枚の画像をリポジトリに含めるとサイズが大きくなる。
Drive の共有ファイルIDから直接URLを組み立てる方式にした。

```
https://lh3.googleusercontent.com/d/{fileId}
```

この方式の利点は、Drive のファイルを差し替えるだけで画像が更新されること。
コード変更もデプロイも不要になる。

**ファイル名をセット定義IDにリネームしてもらった。**
当初は商品名がファイル名だったため自動マッチングが困難だった。
DAICHU 側でセットIDにリネームしてもらい、機械的に対応付けられるようにした。

それでも命名の揺れ（`wii_4p_mariopartyset` と `wii4pmariopartyset` など）があったため、
`normalizeKey()` で記号と大小文字を無視した正規化マッチを実装。
それでも合わない25件はエイリアスとして明示的に登録した。

### ⚠ 誤った判断と訂正

**ピッキング画面にも画像を出したが、かえって混乱を招いた。**

当初「発払いのカードモードで画像を表示」という要望に応えて実装したが、
PS2 カードには厚型・薄型・HDMI変換セットが混在するのに**画像は1枚しか出せない**。
結果、厚型の画像だけが出て「ピッキングの際にむしろ間違いが多く起きそう」という指摘を受けた。

**訂正**: ピッキングのカードモードから画像表示を削除した。

**判断の根拠**: ピッキングは部品単位で棚から集める作業であり、
セット商品の完成形画像はこの作業を助けない。
商品の外観確認が本当に必要なのは梱包の段階なので、そちらだけに残した。

---

## 2026-08-07 電池セット / ポケモン電池交換対応（F1）

### 何を変えたか

- `wiinomalbattset0001` は属性1の「4本」の有無で単三電池の数量を変える
- `wii-balanceboadset01` に単三電池×4 を追加
- ポケモン13商品を「ポケモン（電池交換）」という特別グループに分離
- 該当商品の梱包時に「電池交換を行いましたか？」を表示

### なぜ変えたか

GB/GBA世代のポケモンソフトは内蔵電池が切れるとセーブできない。
DAICHU では出荷前に電池交換を行うルールだが、
初めて梱包する人がそれを知らずに発送してしまうことがあった。

### 判断の根拠

**ポケモンは通常のプラットフォーム分類とは別枠にした。**
GB/GBA に混ぜると他のソフトと区別がつかない。
`POKEMON_BATTERY_GROUP` という専用グループを作り、
ピッキング画面で黄色いヘッダと「まとめて電池交換してください」の注意書きを表示する。

**電池本数は属性から動的に決める。**
`wiinomalbattset0001` には「シロ(電池2本)」「シロ(電池4本)」等のバリエーションがあり、
商品コードが同一のため定義を分けられない。属性1の文字列で判定する方式にした。

---

## 2026-08-06 セット定義のマルチコード対応（Step 4）

### 何を変えたか

`SetDefinition` の構造を変更した。

```typescript
// 変更前
{ productCode: string; matchType: "exact" | "prefix"; ... }

// 変更後
{ id: string; codes: string[]; prefixes: string[]; ... }
```

全モール（楽天・Yahoo・Amazon・メルカリ）の商品データから抽出した
69個の商品コードを既存定義に追加した。

### なぜ変えたか

実データテストで「セット商品なのに同梱物チェックが出ない」問題が多発した。
原因を調べると、**同じセット商品でもモールごとに商品コードが全く違う**ことが判明した。

```
PS2厚型すぐ遊べるセット
  ps210-390001            （.md原典）
  PS210000-30000-00001    （楽天）
  ps2atsugatacolornml     （CROSS MALL CSV）
```

### 判断の根拠

**「1商品コード = 1定義」を捨て、「1セットに複数コード」に変えた。**

採用しなかった案:
- **モールごとに定義を複製** → 同梱物を変更するとき全部直す必要があり、破綻する
- **コード変換テーブルを別に持つ** → 参照が2段階になり、設定画面での編集が複雑になる

`codes: string[]` にしたことで、新しいモールやコード体系が増えても
設定画面からコードを1つ追加するだけで対応できるようになった。

**大文字小文字を区別しない照合にした。**
楽天のコードは大文字（`PS210000-...`）、CROSS MALL は小文字が多い。
`findSetDefinition` で両方を `toLowerCase()` してから比較する。

---

## 2026-08-06 実データテストによる大規模修正（Step 1〜3）

### 何を変えたか

| # | 修正 | ファイル |
|---|---|---|
| 1 | プラットフォーム判定の修正（PS5追加、カタカナ「スイッチ」対応、小文字対応） | platformDetector.ts |
| 2 | 全角/半角スペースの正規化 | parsers.ts |
| 3 | ピッキング集約キーにカラーを含める | parsers.ts |
| 4 | 品目が短い場合は商品名にフォールバック | parsers.ts |
| 5 | 戻るボタンでCSVデータを消さないように | App.tsx |
| 6 | ピッキング未完了でも梱包へ進めるスキップボタン | App.tsx |

### なぜ変えたか

実データ（176件）でテストしたところ、多数の問題が発覚した。

### 判断の根拠

**カラーは本体・コントローラー類だけ集約キーに含める。**

変更前は `3DS本体 ×3` としか出ず、何色を何個取ればいいか分からなかった。

かといって全部品にカラーを付けると、`HDMIケーブル(ブラック) ×2` `HDMIケーブル(ホワイト) ×1` のように
本来まとめられるものが分裂してしまう。

`isColorRelevant()` で「本体」「コントローラ」「DUALSHOCK」「Joy-Con」「リモコン」を
含む部品名だけカラーを付ける方式にした。

**スペース正規化は必須だった。**
`PS2 本体 【すぐ遊べるセッ` と `PS2 本体　【すぐ遊べるセッ`（全角スペース）が
別アイテムとして2行に分かれていた。現場からも「何が違うのかわからず致命的」と指摘があった。

---

## 2026-08-05 初期実装

### 何を作ったか

中国輸入部門の梱包サポートツールをベースに、ゲーム・リサイクル部門向けに再構築した。

### 中国輸入版から削除したもの

| 削除 | 理由 |
|---|---|
| `productLinks.ts`（商品URL辞書） | 商品数が多く、日々増えるため管理不可能 |
| 佐川急便のキャリア分岐 | ゲーム部門はヤマトのみ |
| `MERGE_CODES` / `SET_PATTERN` | 中国輸入部門固有のロジック |
| サンダル保管シート・水着バッジ | 中国輸入部門固有 |
| カラースウォッチ強調 | ゲーム部門では不要 |

### 新規に作ったもの

| 追加 | 目的 |
|---|---|
| `platformDetector.ts` | 棚がハード別に整理されているため、プラットフォームでグルーピングする |
| `setDefinitions.ts` | セット商品の同梱物を定義・管理する |
| `SettingsScreen.tsx` | 現場がセット定義を編集できるようにする |

### 判断の根拠 ★設計の核心

**棚番（CSV列39）は使わず、プラットフォーム判定でグルーピングする。**

CROSS MALL には棚番の列があるが、A/C/S といった値が入るだけで実務上機能していない。
一方、実際の倉庫では**棚がハード別に整理されている**。
そこで品目・商品コードからプラットフォームを判定してグルーピングする方式にした。

**セット商品は同梱物に分解して集計する。**

現場の要望はこうだった:

> PS3のすぐ遊べるセットが2セットある場合、実質的にピッキングしなければならないのは
> PS3本体が2つ、HDMIケーブルが2つ、電源用の三芯ケーブルが2つ…

つまりセット単位ではなく**部品単位**で集めたい。
そこで `buildPickingItems()` でセットを部品に分解し、部品名で集約する設計にした。

**集約キーは「正規化された部品名」。**

ここが最大の設計判断だった。元の同梱物リストは「電源ケーブル」「USBケーブル」など
汎用的な名前で書かれていたが、実際には物理的に別物だった。

```
PS3初期型の電源ケーブル = 三芯ケーブル
PS3中期型の電源ケーブル = メガネケーブル
PS3のUSBケーブル       = miniB（太い）
PS4のUSBケーブル       = microB（細い）
```

そのまま集約すると「電源ケーブル ×7」となり、何を7本取ればいいか分からない。
DAICHU と協議して**正規化テーブル**を作成し、
物理的に同一の部品には同じ名前を付ける方針を確定した。

これにより「メガネケーブル ×6」「三芯ケーブル ×1」と正しく分離され、
かつ PS1・PS2・PS3・PS4 をまたいで合算されるようになった。

---

## 付録: 繰り返さないための教訓

この開発で実際に踏んだ落とし穴。

1. **App.tsx への部分的な差分編集は JSX 構造を壊しやすい。**
   閉じタグの欠落・import の重複が複数回発生した。関数やブロック単位で置き換える。
   編集後は必ず `npm run build` を通す。

2. **判定関数は順序が命。**
   `Switch2` → `Switch`、`PS5` → `PS4` → … の順に評価しないと、
   短いプレフィックスが先にマッチしてしまう。

3. **同一商品コードで属性違いがある場合、チェックキーに商品インデックスが必要。**
   PSP-3000を3色購入されたとき、キーが `psp3000-hontai_PSP本体_0` で重複し、
   1つ目にチェックを入れると2つ目3つ目も連動する事故が起きた。

4. **推測でファイル内容を判断しない。**
   スクリーンショットの対応付けを目視確認せずに推測し、全体をずらす誤りを犯した。

5. **Tailwind の動的クラス名は効かない。**
   `border-${color}-500` は静的解析で拾えない。完全な文字列で書く。

6. **localStorage に新フィールドを足したら後方互換の補完を入れる。**
   既存データを読んだときに `undefined` でクラッシュする。